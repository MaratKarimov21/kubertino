# Тестовые Сценарии - Story 3.1: Отображение Списка Подов

## Дата: 2025-10-03
## Версия: 1.0

---

## 🎯 Основные Сценарии

### Сценарий 1: Базовый Happy Path - Просмотр подов в namespace
**Цель:** Проверить основной поток отображения подов

**Предусловия:**
- Kubertino запущен
- Выбран контекст с доступом к кластеру
- В кластере есть namespace с запущенными подами

**Шаги:**
1. Запустить `./kubertino` (или `go run cmd/kubertino/main.go`)
2. Если несколько контекстов - выбрать рабочий контекст (стрелки + Enter)
3. Дождаться загрузки списка namespace
4. Выбрать namespace с подами (например, `default` или `kube-system`)
5. Нажать Enter

**Ожидаемый результат:**
- ✅ Появляется индикатор "Loading pods..." в правой верхней панели
- ✅ Через 1-2 секунды отображается список подов
- ✅ Каждый под показан в формате: `STATUS  pod-name`
- ✅ Статусы раскрашены:
  - 🟢 `Running` / `Succeeded` - зелёный
  - 🟡 `Pending` - жёлтый
  - 🔴 `Failed` - красный
  - ⚪ `Unknown` - серый

**Скриншот для проверки:**
```
┌─ Namespaces (3) ─────────┐  ┌─ Pods ────────────────────┐
│ > default                 │  │ Running     nginx-5d9...   │
│   kube-system             │  │ Running     postgres-7a... │
│   production              │  │ Pending     worker-2b8...  │
│                           │  │                            │
└───────────────────────────┘  └────────────────────────────┘
```

---

### Сценарий 2: Пустой Namespace - Empty State
**Цель:** Проверить отображение когда в namespace нет подов

**Предусловия:**
- Есть доступ к namespace без подов (или создать: `kubectl create ns empty-test`)

**Шаги:**
1. Запустить kubertino
2. Выбрать пустой namespace
3. Нажать Enter

**Ожидаемый результат:**
- ✅ Индикатор загрузки появляется
- ✅ После загрузки показывается сообщение: "No pods in this namespace"
- ✅ Сообщение отображается серым курсивом
- ❌ НЕ должно быть ошибки или пустого экрана

---

### Сценарий 3: Переключение между Namespace
**Цель:** Проверить что поды обновляются при смене namespace

**Шаги:**
1. Выбрать namespace `default` (Enter)
2. Запомнить список подов в правой панели
3. Выбрать другой namespace (стрелка вниз + Enter)
4. Наблюдать за правой панелью

**Ожидаемый результат:**
- ✅ При переключении старые поды исчезают
- ✅ Показывается "Loading pods..."
- ✅ Загружается новый список подов для выбранного namespace
- ✅ Список соответствует выводу `kubectl get pods -n <namespace>`

**Команда для проверки:**
```bash
kubectl get pods -n default
kubectl get pods -n kube-system
```

---

### Сценарий 4: Поиск Namespace с последующим просмотром подов
**Цель:** Проверить что поды загружаются после поиска namespace

**Шаги:**
1. Нажать `/` для активации поиска
2. Ввести часть имени namespace (например, `kube`)
3. Нажать Enter на найденном namespace

**Ожидаемый результат:**
- ✅ Поиск работает (подсвечивается совпадение)
- ✅ После Enter режим поиска закрывается
- ✅ Загружаются поды для выбранного namespace
- ✅ Правая панель обновляется корректно

---

## ⚠️ Негативные Сценарии

### Сценарий 5: Ошибка доступа - Permission Denied
**Цель:** Проверить обработку ошибки прав доступа

**Предусловия:**
- Контекст с ограниченными правами (RBAC запрещает `get pods`)

**Шаги:**
1. Выбрать namespace без прав на просмотр подов
2. Нажать Enter

**Ожидаемый результат:**
- ✅ Показывается сообщение об ошибке красным текстом
- ✅ Текст содержит "Error:" и описание проблемы
- ✅ Примерный текст: "Error: permission denied" или "Error: Forbidden"
- ✅ Приложение не падает, можно выбрать другой namespace

**Как симулировать:**
```bash
# Создать namespace с ограничениями (если есть права)
kubectl create ns restricted-ns
# Или использовать контекст с ограниченным ServiceAccount
```

---

### Сценарий 6: Таймаут соединения с кластером
**Цель:** Проверить обработку сетевых проблем

**Предусловия:**
- Контекст указывает на недоступный кластер

**Как симулировать:**
```bash
# Временно изменить ~/.kube/config
# Добавить контекст с недоступным сервером:
# server: https://192.168.99.999:6443
```

**Шаги:**
1. Выбрать контекст с недоступным кластером
2. Попытаться выбрать namespace

**Ожидаемый результат:**
- ✅ Через ~10 секунд показывается ошибка таймаута
- ✅ Сообщение: "Error: operation timeout" или "connection timeout"
- ✅ Можно вернуться и выбрать другой контекст (q для выхода)

---

### Сценарий 7: Несуществующий Namespace
**Цель:** Проверить что происходит если namespace был удалён

**Предусловия:**
- Namespace отображается в списке, но будет удалён во время работы

**Шаги:**
1. Запустить kubertino, увидеть namespace в списке
2. В другом терминале: `kubectl delete ns test-namespace`
3. В kubertino выбрать удалённый namespace

**Ожидаемый результат:**
- ✅ Показывается ошибка: "Error: namespace not found"
- ✅ Приложение остаётся работоспособным
- ✅ Можно обновить список namespace (возможно в будущих story)

---

## 🎨 UI/UX Сценарии

### Сценарий 8: Проверка цветовой кодировки статусов
**Цель:** Убедиться что все статусы отображаются корректными цветами

**Предусловия:**
- Кластер с подами в разных состояниях

**Как создать разные статусы:**
```bash
# Running pod
kubectl run nginx --image=nginx

# Pending pod (неправильный image)
kubectl run pending-test --image=nonexistent:tag

# Failed pod
kubectl run failing --image=busybox --restart=Never -- /bin/sh -c "exit 1"

# Succeeded pod
kubectl run success --image=busybox --restart=Never -- /bin/sh -c "echo done"
```

**Проверка:**
1. Выбрать namespace с разными статусами
2. Визуально проверить цвета:

**Ожидаемый результат:**
```
Pods
Running     nginx           <- ЗЕЛЁНЫЙ + жирный
Pending     pending-test    <- ЖЁЛТЫЙ + жирный
Failed      failing         <- КРАСНЫЙ + жирный
Succeeded   success         <- ЗЕЛЁНЫЙ + жирный
Unknown     mystery-pod     <- СЕРЫЙ (обычный)
```

---

### Сценарий 9: Длинные имена подов
**Цель:** Проверить что длинные имена не ломают UI

**Предусловия:**
- Под с очень длинным именем (Kubernetes позволяет до 253 символов)

**Как создать:**
```bash
kubectl run very-long-pod-name-that-might-cause-ui-issues-if-not-handled-properly-testing-edge-case --image=nginx
```

**Проверка:**
1. Выбрать namespace с длинным именем
2. Проверить отображение

**Ожидаемый результат:**
- ✅ Имя либо полностью помещается в панель, либо обрезается с `...`
- ✅ Панель не "ломается" и не наезжает на другие элементы
- ✅ Границы панелей остаются на месте

---

### Сценарий 10: Множество подов (>20)
**Цель:** Проверить отображение большого списка подов

**Предусловия:**
- Namespace с большим количеством подов (например, `kube-system`)

**Шаги:**
1. Выбрать `kube-system` или другой namespace с множеством подов
2. Наблюдать отображение

**Ожидаемый результат:**
- ✅ Все поды отображаются в панели
- ✅ Если не помещаются - появляется прокрутка или индикатор
- ✅ UI остаётся читаемым
- ✅ Производительность приемлемая (загрузка <2 сек)

---

## 🔄 Сценарии Интеграции

### Сценарий 11: Быстрое переключение между namespace
**Цель:** Проверить стабильность при частом переключении

**Шаги:**
1. Быстро переключаться между namespace (Enter, стрелка, Enter, стрелка...)
2. Делать ~10 переключений подряд

**Ожидаемый результат:**
- ✅ Каждое переключение корректно загружает поды
- ✅ Нет "застывших" состояний загрузки
- ✅ Нет race conditions (некорректных данных)
- ✅ Приложение не падает и не виснет

---

### Сценарий 12: Сравнение с kubectl
**Цель:** Убедиться что данные совпадают с kubectl

**Шаги:**
1. Выбрать namespace в kubertino
2. В другом терминале: `kubectl get pods -n <namespace>`
3. Сравнить списки

**Ожидаемый результат:**
- ✅ Количество подов совпадает
- ✅ Имена подов совпадают
- ✅ Статусы подов совпадают
- ✅ Порядок может отличаться (это OK)

**Команды для сравнения:**
```bash
# В отдельном терминале
kubectl config use-context <your-context>
kubectl get pods -n default
kubectl get pods -n kube-system -o wide
```

---

## 📊 Тестовая Матрица

| Сценарий | AC Покрытие | Приоритет | Время |
|----------|-------------|-----------|-------|
| #1 Базовый Happy Path | AC 1,2,3,4 | 🔴 P0 | 2 мин |
| #2 Пустой Namespace | AC 6 | 🔴 P0 | 1 мин |
| #3 Переключение NS | AC 1,7 | 🔴 P0 | 2 мин |
| #4 Поиск + Просмотр | AC 1,7 | 🟡 P1 | 2 мин |
| #5 Permission Denied | AC 5 | 🔴 P0 | 3 мин |
| #6 Таймаут | AC 5 | 🟡 P1 | 5 мин |
| #7 Несуществующий NS | AC 5 | 🟡 P1 | 2 мин |
| #8 Цвета статусов | AC 3 | 🔴 P0 | 3 мин |
| #9 Длинные имена | AC 2 | 🟢 P2 | 2 мин |
| #10 Много подов | AC 2 | 🟡 P1 | 2 мин |
| #11 Быстрое переключение | AC 7 | 🟡 P1 | 3 мин |
| #12 Сравнение с kubectl | AC 1,2,3 | 🔴 P0 | 3 мин |

**Итого:** ~30 минут для полного прохода

---

## ✅ Чек-лист для Тестирования

### Перед началом:
- [ ] Кластер доступен (`kubectl cluster-info`)
- [ ] В кластере есть несколько namespace с подами
- [ ] Kubertino собран (`go build -o kubertino cmd/kubertino/main.go`)
- [ ] Терминал достаточного размера (минимум 80x24)

### Обязательные проверки (P0):
- [ ] ✅ Базовый сценарий работает (поды отображаются)
- [ ] ✅ Пустой namespace показывает "No pods"
- [ ] ✅ Переключение между namespace обновляет список
- [ ] ✅ Ошибки отображаются красным текстом
- [ ] ✅ Цвета статусов корректные (зелёный/жёлтый/красный)
- [ ] ✅ Данные совпадают с kubectl

### Дополнительные проверки (P1):
- [ ] Поиск работает с просмотром подов
- [ ] Таймаут обрабатывается (не зависает навсегда)
- [ ] Несуществующий namespace показывает ошибку
- [ ] Много подов отображается корректно
- [ ] Быстрое переключение стабильно

### Nice-to-have (P2):
- [ ] Длинные имена не ломают UI
- [ ] Форматирование аккуратное
- [ ] Отступы и выравнивание правильные

---

## 🐛 Шаблон для Репорта Багов

Если найдёте проблему, используйте этот формат:

```markdown
## Bug Report

**Сценарий:** #X - Название сценария
**Шаг:** Номер шага где произошла ошибка
**Ожидалось:** Что должно было произойти
**Получено:** Что произошло на самом деле
**Воспроизведение:** Шаги для повторения
**Скриншот/Лог:** (если есть)
**Критичность:** P0/P1/P2
```

---

## 📝 Примечания

1. **Контексты:** Если у вас один контекст, экран выбора контекста пропускается автоматически
2. **Namespace:** Favourites помечаются звёздочкой ★ и отображаются первыми
3. **Производительность:** Загрузка подов должна занимать <2 секунд для обычных namespace
4. **Цвета:** Если терминал не поддерживает цвета, статусы всё равно должны быть читаемыми

---

## 🎯 Готово к тестированию!

Начните с обязательных сценариев (P0), затем переходите к дополнительным.
Удачного тестирования! 🚀
